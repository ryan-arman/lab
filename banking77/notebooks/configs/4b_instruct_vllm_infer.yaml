# vLLM Inference config for Qwen3-4B-Instruct-2507 (Banking77 Classification).
#
# Requirements:
#   - Run `pip install oumi[gpu]`
#
# Usage:
#   oumi infer -i -c notebooks/4b_instruct_vllm_infer.yaml
#
# See Also:
#   - Documentation: https://oumi.ai/docs/en/latest/user_guides/infer/infer.html
#   - Config class: oumi.core.configs.InferenceConfig
#   - Config source: https://github.com/oumi-ai/oumi/blob/main/src/oumi/core/configs/inference_config.py
#   - Other inference configs: configs/**/inference/

model:
  model_name: "Qwen/Qwen3-4B-Instruct-2507"
  model_max_length: 32768
  torch_dtype_str: "bfloat16"
  attn_implementation: "sdpa"
  trust_remote_code: True
  # NOTE: For LoRA adapters, adapter_model will be set via command line
  # adapter_model: null  # Overridden by --model.adapter_model

generation:
  max_new_tokens: 100  # Enough for a label id
  temperature: 0.0  # Deterministic for classification
  use_sampling: False  # Deterministic

engine: VLLM

